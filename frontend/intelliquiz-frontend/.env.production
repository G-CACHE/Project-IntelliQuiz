# Use empty string for relative URLs - works from any IP (localhost or LAN)
# Frontend will call /api/* and nginx will proxy to backend container
# This allows: http://localhost:3000, http://10.243.101.147:3000, etc.
VITE_API_BASE_URL=
